# Borrow Buddy: เอกสารออกแบบ (เวอร์ชัน 2)

ศัพท์ทั้งหมดในเอกสารนี้อ้างอิงจาก [CONTEXT.md](./CONTEXT.md)

เอกสารนี้ปรับปรุงจากเวอร์ชัน 1 (เก็บข้อมูลใน localStorage ไม่มีล็อกอิน) เป็นเวอร์ชัน 2: ย้ายข้อมูลไป Supabase, เพิ่มล็อกอินสำหรับเจ้าของ, และใช้ Row Level Security (RLS) จำกัดให้เห็นเฉพาะ Loan ของตัวเอง ส่วนที่ไม่ได้กล่าวถึงในเอกสารนี้ถือว่ายังเป็นไปตามเวอร์ชัน 1

## 1. ภาพรวมและเป้าหมาย
เว็บหน้าเดียวสำหรับ **เจ้าของ** คนเดียว ใช้บันทึกว่า **เพื่อน** ยืม **ของ** อะไร เมื่อไร ต้องคืนเมื่อไร และกดว่า **คืนแล้ว** ได้ เจ้าของต้องล็อกอินก่อนจึงจะใช้งานได้

## 2. ขอบเขต

**อยู่ในขอบเขต**
- เพิ่ม Loan, แก้ไข Loan, ดูรายการ Loan
- กดคืนแล้ว (ระบุวันที่คืนจริง) และยกเลิกการคืน
- ค้นหา/กรองตามชื่อเพื่อน
- แสดงสถานะ และจำนวนวันเกินกำหนด
- ล็อกอินด้วยอีเมลและรหัสผ่านสำหรับเจ้าของ (ไม่เปิดให้สมัครสมาชิกเอง บัญชีสร้างไว้ล่วงหน้า)
- Loan ของเจ้าของแต่ละคนแยกจากกัน มองเห็นเฉพาะของตัวเองเท่านั้น (บังคับด้วย RLS)

**นอกขอบเขต (เวอร์ชัน 2)**
- ลบ Loan, แจ้งเตือน, เลื่อนกำหนดคืน, รูปภาพของ
- ทะเบียนรายชื่อเพื่อน/ของแยก, การตรวจของซ้ำซ้อน
- สำรองข้อมูล/นำเข้า-ส่งออกไฟล์
- การสมัครสมาชิกเอง (self sign-up), ลืมรหัสผ่าน/รีเซ็ตรหัสผ่านในหน้าเว็บ (จัดการบัญชีผ่าน Supabase Dashboard โดยตรง)
- หลายเจ้าของใช้บัญชีเดียวกัน/แชร์สิทธิ์เข้าถึง Loan ระหว่างเจ้าของ

## 3. เทคโนโลยี
- React + Vite, ภาษา JavaScript (ไม่ใช้ TypeScript)
- Supabase (Postgres + Auth) เก็บข้อมูล Loan และจัดการล็อกอินเจ้าของ ผ่าน `@supabase/supabase-js`
- ทดสอบด้วย Vitest (ตรรกะล้วนใน `lib/` เหมือนเดิม, ส่วนที่เรียก Supabase ใช้ mock client ในเทสต์)

## 4. โมเดลข้อมูล

**Loan**
| ฟิลด์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | uuid | สร้างอัตโนมัติโดย Supabase |
| `owner_id` | uuid | อ้างอิงบัญชีเจ้าของ (`auth.uid()`) ใช้กับ RLS เท่านั้น ไม่แสดงในหน้าเว็บ |
| `friendName` | string | ชื่อเล่นของเพื่อน พิมพ์เองทุกครั้ง ห้ามว่าง |
| `itemName` | string | ชื่อของ ห้ามว่าง |
| `borrowedDate` | string `YYYY-MM-DD` | วันที่ยืม |
| `dueDate` | string `YYYY-MM-DD` | กำหนดคืน บังคับกรอก |
| `returnedDate` | string `YYYY-MM-DD` หรือ `null` | วันที่คืนจริง ว่างจนกว่าจะกดคืน |

**บัญชีเจ้าของ (Owner Account)**
จัดการโดย Supabase Auth (`auth.users`) ยืนยันตัวตนด้วยอีเมล + รหัสผ่าน ไม่มีข้อมูลอื่นนอกจากที่ Supabase Auth เก็บมาตรฐาน บัญชีสร้างไว้ล่วงหน้า ไม่เปิดให้สมัครสมาชิกเอง

วันที่เก็บเป็น ค.ศ. แบบ ISO และแสดงผลเป็น พ.ศ. เท่านั้น (เช่น 24 ก.ย. 2569) สถานะและจำนวนวันเกินกำหนดไม่เก็บ คำนวณจากวันที่ทุกครั้ง

## 5. กติกา

**สถานะ** (คำนวณจากวันนี้)
- **คืนแล้ว**: `returnedDate` ไม่เป็น null
- **เกินกำหนด**: ยังไม่คืน และวันนี้ > `dueDate`
- **ยังไม่คืน**: ยังไม่คืน และวันนี้ ≤ `dueDate` (ถึงกำหนดวันนี้พอดียังไม่เกิน)

**จำนวนวันเกินกำหนด** = วันนี้ − `dueDate` (นับเป็นวัน ใช้กับ Loan ที่เกินกำหนดเท่านั้น)

**การตรวจวันที่** (ปฏิเสธการบันทึกเมื่อผิดกติกา พร้อมข้อความภาษาไทย)
- `dueDate` ต้องไม่ก่อน `borrowedDate`
- `returnedDate` ต้องไม่ก่อน `borrowedDate`
- ชื่อเพื่อนและชื่อของห้ามว่าง

**การกดคืนแล้ว**
- ค่าเริ่มต้นของวันที่คืนจริงคือวันนี้ แก้เป็นวันย้อนหลังได้
- ยกเลิกการคืนได้ (ตั้ง `returnedDate` กลับเป็น null)

**การเข้าถึงข้อมูล**
- ต้องล็อกอินก่อนจึงจะเห็น/เพิ่ม/แก้ไข Loan ได้
- เห็นเฉพาะ Loan ที่ `owner_id` ตรงกับบัญชีที่ล็อกอินอยู่ บังคับด้วย RLS ที่ชั้นฐานข้อมูล ไม่ใช่แค่ซ่อนใน UI

## 6. หน้าจอ

- **หน้าล็อกอิน**: ช่องกรอกอีเมลและรหัสผ่าน ปุ่มเข้าสู่ระบบ แสดงข้อความผิดพลาดภาษาไทยเมื่อล็อกอินไม่สำเร็จ ไม่มีลิงก์สมัครสมาชิก แสดงก่อนเข้าหน้าหลักเสมอถ้ายังไม่ได้ล็อกอิน
- **หน้าหลัก** (หลังล็อกอินแล้ว หน้าเดียวเหมือนเวอร์ชัน 1 บวกปุ่มออกจากระบบในส่วนหัว):
- **ฟอร์ม Loan** ด้านบน ใช้ทั้งเพิ่มและแก้ไข: เพื่อน, ของ, วันที่ยืม (ค่าเริ่มต้นวันนี้), กำหนดคืน กดแก้ไขที่รายการแล้วข้อมูลขึ้นฟอร์ม มีปุ่มยกเลิกการแก้ไข
- **ช่องค้นหา/กรอง** ตามชื่อเพื่อน
- **รายการ Loan** ด้านล่าง แยกกลุ่มตามลำดับ **เกินกำหนด → ยังไม่คืน → คืนแล้ว** แต่ละกลุ่มเรียงตามกำหนดคืนใกล้สุดก่อน
- แต่ละรายการแสดง: ชื่อของ, ชื่อเพื่อน, วันที่ยืม, กำหนดคืน, สถานะ, จำนวนวันเกินกำหนด (เฉพาะเกินกำหนด), วันที่คืนจริง (เฉพาะคืนแล้ว)
- ปุ่มต่อรายการ: **คืนแล้ว** (เฉพาะที่ยังไม่คืน), **ยกเลิกการคืน** (เฉพาะที่คืนแล้ว), **แก้ไข**
- ไม่มีปุ่มลบ

**การแสดงผล**
- ภาษาไทยทั้งหมด, วันที่ พ.ศ.
- รองรับหน้าจอมือถือ (layout ยืดหยุ่น)
- โหมดมืด สลับได้ด้วยปุ่ม และจำค่าที่เลือกไว้
- ใช้สี/ข้อความแยกสถานะให้ชัด ไม่พึ่งสีอย่างเดียว

## 7. โครงสร้างโค้ด (ระดับโฟลเดอร์)

แยกตรรกะล้วนออกจาก UI เพื่อให้ทดสอบง่าย

```
borrow-buddy/
├── CONTEXT.md
├── design.md
├── package.json
├── index.html
└── src/
    ├── main.jsx
    ├── App.jsx              # ตรวจสถานะล็อกอิน, ประกอบหน้า, เก็บ state ของ Loan
    ├── components/
    │   ├── LoginForm.jsx    # ฟอร์มล็อกอิน (อีเมล + รหัสผ่าน)
    │   ├── LoanForm.jsx
    │   ├── LoanList.jsx
    │   ├── LoanItem.jsx
    │   ├── SearchBox.jsx
    │   └── ThemeToggle.jsx
    └── lib/
        ├── loanRules.js     # สถานะ, จำนวนวันเกิน, ตรวจวันที่, เรียง/จัดกลุ่ม
        ├── dateFormat.js    # แปลง ISO ค.ศ. เป็นข้อความ พ.ศ.
        ├── supabaseClient.js # สร้าง Supabase client จาก env vars
        └── storage.js       # อ่าน/เขียน Loan ผ่าน Supabase (จำกัดเฉพาะของเจ้าของด้วย RLS)
```

ฟังก์ชันใน `lib/` รับ "วันนี้" เป็นพารามิเตอร์ ไม่เรียกเวลาจริงข้างใน เพื่อให้ทดสอบได้แน่นอน

## 8. การเก็บข้อมูล
- ใช้ Supabase (Postgres) ตาราง `loans` แทน localStorage เก็บคอลัมน์ตามโมเดลข้อมูลในข้อ 4 (รวม `owner_id`)
- เปิด Row Level Security (RLS) บนตาราง `loans`; policy SELECT/INSERT/UPDATE อนุญาตเฉพาะแถวที่ `owner_id = auth.uid()`
- ไม่สร้าง policy สำหรับ DELETE บนตาราง `loans` เพื่อคงข้อจำกัด "ยังไม่มีการลบ Loan" ไว้ที่ชั้นฐานข้อมูล ไม่ใช่แค่ซ่อนปุ่มใน UI
- ล็อกอินด้วย Supabase Auth (อีเมล + รหัสผ่าน) ปิด public sign-up ใน Supabase Auth settings บัญชีเจ้าของสร้างล่วงหน้าโดยตรงผ่าน Supabase Dashboard
- เซสชันล็อกอินเก็บโดย Supabase client เอง (อาจใช้ localStorage เก็บ session token เท่านั้น ไม่ใช่ข้อมูล Loan)
- บันทึกทุกครั้งที่ Loan เปลี่ยน โหลดตอนเปิดหน้า (เหมือนเวอร์ชัน 1 แต่เปลี่ยนปลายทางเป็น Supabase)
- ถ้าเรียก Supabase ไม่สำเร็จ (เครือข่ายล่ม, session หมดอายุ, สิทธิ์ไม่พอ) ให้แจ้งเจ้าของเป็นภาษาไทย ห้ามเขียนทับข้อมูลเดิมโดยไม่แจ้ง
- ข้อมูลอยู่บนคลาวด์ (Supabase) ไม่ผูกกับเครื่อง/เบราว์เซอร์เดียวอีกต่อไป เข้าถึงได้จากอุปกรณ์ใดก็ได้หลังล็อกอินด้วยบัญชีเดิม

## 9. การทดสอบ (Vitest)
- ตรรกะล้วนใน `loanRules.js` และ `dateFormat.js` ทดสอบเหมือนเวอร์ชัน 1 (pure function ไม่พึ่ง Supabase):
  - สถานะ: ยังไม่คืน / เกินกำหนด / คืนแล้ว
  - ขอบเขต: วันนี้ = กำหนดคืน ต้องเป็นยังไม่คืน, วันถัดไปต้องเป็นเกินกำหนด
  - จำนวนวันเกินกำหนด
  - การตรวจวันที่ทุกกรณีที่ผิดกติกา
  - การจัดกลุ่มและเรียงลำดับ
  - การแปลงวันที่เป็น พ.ศ.
  - การกรองตามชื่อเพื่อน
- `storage.js` ทดสอบด้วยการ mock Supabase client (ไม่ยิง network จริง): ตรวจว่าเรียก query ที่ถูกต้อง และจัดการกรณีเรียกไม่สำเร็จตามข้อ 8
- ไม่ทดสอบ RLS policy ด้วย Vitest เพราะเป็นเรื่องของฐานข้อมูล ตรวจสอบด้วยบัญชีทดสอบจริงผ่าน Supabase แทน (ดู Tasks.md เฟส 5)

## 10. ประเด็นที่ตั้งใจไม่ทำ
สำรองข้อมูล, ลบ Loan, แจ้งเตือน, เลื่อนกำหนด, รูปภาพ, ทะเบียนเพื่อน/ของ, การสมัครสมาชิกเอง, ลืมรหัสผ่านในหน้าเว็บ, หลายเจ้าของ/แชร์บัญชี หากต้องการในอนาคตให้ทบทวน CONTEXT.md ก่อน
